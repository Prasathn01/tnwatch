# frontend/

Next.js (App Router) + TypeScript + Tailwind, shipped as a PWA.

Pages to build (Slice #1):
- /              home: state of the assembly, top movers
- /mlas          searchable/filterable list of 234 MLAs
- /mlas/[id]     MLA detail: profile + score breakdown + sourced links
- /constituency/[id]  constituency view

Charts: Recharts. Map: Mapbox GL. Deploy: Vercel.
